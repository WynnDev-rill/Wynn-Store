import { Card } from '../components/ui/Card'; import { EmptyState } from '../components/ui/States'; export default function HistoryPage(){return <section className="container-page py-14"><h1 className="text-3xl font-extrabold">Riwayat Transaksi</h1><p className="mt-2 text-muted">Semua transaksi top up kamu akan tampil di sini.</p><Card className="mt-8"><EmptyState message="Belum ada riwayat transaksi"/></Card></section>}
