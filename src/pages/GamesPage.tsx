import { Search } from 'lucide-react'; import { useMemo, useState } from 'react'; import { GameCard } from '../components/game/GameCard'; import { Input } from '../components/ui/Input'; import { games } from '../data/games';
export default function GamesPage(){const [query,setQuery]=useState('');const filtered=useMemo(()=>games.filter(g=>g.title.toLowerCase().includes(query.toLowerCase())),[query]);return <section className="container-page py-14"><div className="max-w-2xl"><p className="text-sm font-bold text-violet-500">KATALOG GAME</p><h1 className="mt-2 text-4xl font-extrabold tracking-tight">Temukan game favoritmu</h1><p className="mt-4 text-muted">Pilih game dan isi ulang kebutuhanmu dengan proses yang praktis.</p></div><label className="relative mt-8 block max-w-lg"><span className="sr-only">Cari game</span><Search className="absolute left-4 top-3.5 text-muted" size={18}/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Cari nama game..." className="pl-11"/></label><div className="mt-10 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6">{filtered.map(g=><GameCard key={g.id} game={g}/>)}</div></section>}
