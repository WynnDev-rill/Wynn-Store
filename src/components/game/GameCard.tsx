import { ArrowUpRight, Star } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { Game } from '../../types'; import { Badge } from '../ui/Badge'; import { Card } from '../ui/Card';
export function GameCard({game}:{game:Game}){return <Link to={`/games/${game.id}`} aria-label={`Lihat ${game.title}`}><Card className="group overflow-hidden transition duration-300 hover:-translate-y-1 hover:border-violet-500/40 hover:shadow-glow"><div className={`relative aspect-[4/3] overflow-hidden bg-gradient-to-br ${game.accent}`}><img src={game.image} alt={`Artwork resmi ${game.title}`} loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-x-0 top-0 h-20 bg-gradient-to-b from-black/55 to-transparent"/><Badge className="absolute left-3 top-3 border-white/20 bg-black/30 text-white backdrop-blur">{game.category}</Badge><span className="absolute right-3 top-3 rounded-full bg-black/30 p-2 text-white backdrop-blur"><ArrowUpRight size={16}/></span></div><div className="p-4"><h3 className="font-bold">{game.title}</h3><p className="mt-1 text-xs text-muted">{game.publisher}</p><div className="mt-3 flex items-center justify-between text-xs"><span className="flex items-center gap-1 text-amber-400"><Star size={13} fill="currentColor"/> {game.rating}</span><span className="text-muted">{game.sold} terjual</span></div></div></Card></Link>}
